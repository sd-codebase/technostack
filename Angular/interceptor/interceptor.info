Intro-
Interceptor is special service that allow us to intercept incoming or outgoing HTTP requests using the HttpClient.
Interceptor can handle both HttpRequest as well as HttpResponse
By intercepting the Http request, we can modify or change the value of the request.
and by intercepting the response, we can perform some predefined actions on a particular status code or message.

We are going to see how interceptor works in angular


Conclude-
when we add multiple interceptors, order of adding interceptors in providers array does matter. They executes one by one as chain